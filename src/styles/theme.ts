import { createTheme } from "@mui/material/styles"; //코어에서!!
export const theme = createTheme({
  palette: {
    primary: {
      // main: teal[300]
      main: "#4db6ac",
      dark: "#212121",
    },
    secondary: {
      // main: indigo[300]
      main: "#5c6bc0",
    },
  },
});
